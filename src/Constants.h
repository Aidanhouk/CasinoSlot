#pragma once

// these variables can be changed
const unsigned int N_SYMBOLS_PER_SPIN = 25u;
const float SPIN_TIME = 3.0f;
const float TIME_BETWEEN_DRUM_SPINS = 0.5f;

// these variables depend on slot interface and images size
const unsigned int N_DRUMS = 5u;
const unsigned int N_ROWS_ON_SCREEN = 3u;
const unsigned int SYMBOL_HEIGHT_PIXELS = 180;